# skills/ — 动态 Skill 系统

- `registry.py` — SkillRegistry：注册、扫描内置/工作区 skill
- `matcher.py` — SkillMatcher：领域 + 触发关键词双匹配
- `parser.py` — Skill 解析器：Markdown + YAML frontmatter
- `builtin/` — 内置 skill（github, skill-creator, summarize, weather）
