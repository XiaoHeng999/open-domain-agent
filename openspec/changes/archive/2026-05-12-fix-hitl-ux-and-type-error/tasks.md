## 1. 类型安全修复

- [x] 1.1 在 `permission.py` 的 `_match_rules` 中为 `url`、`command`、`path` 参数值添加 `str()` 类型保护，确保 `in` 和 `fnmatch` 操作的参数为字符串
- [x] 1.2 在类型转换处添加 `logger.debug` 记录非字符串参数的情况，方便调试
- [x] 1.3 运行现有测试验证修改不破坏正常路径

## 2. HITL 审批提示重构

- [x] 2.1 新增 `_format_approval_prompt` 方法，构建结构化审批提示（风险级别标签、操作名称、目标路径/URL）
- [x] 2.2 实现 `_truncate_value` 辅助方法，对超过 100 字符的参数值进行截断
- [x] 2.3 重构 `_ask_human` 方法，支持 `[y]es / [n]o / [d]etail` 三种输入，输入 `d/detail` 时显示完整参数后重新提示
- [x] 2.4 确保非交互模式（`interactive=False`）和 `except` 分支的行为不变：默认拒绝

## 3. 集成验证

- [x] 3.1 手动测试：执行写操作触发审批提示，验证提示展示效果（风险级别、操作描述、输入引导）
- [x] 3.2 手动测试：输入 `d` 查看详情，验证完整参数展示后重新提示
- [x] 3.3 手动测试：构造非字符串参数的工具调用，验证不再抛出 TypeError
- [x] 3.4 运行完整测试套件，确保无回归
